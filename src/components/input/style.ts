import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    input: {
        backgroundColor: '#07111C',
        color: '#FFFFFF',
        borderWidth: 1,
        borderColor: '#243447',
        borderRadius: 16,
        padding: 16,
        marginBottom: 14,
        fontSize: 15,
    },
});